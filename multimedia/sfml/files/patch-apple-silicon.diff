--- CMakeLists.txt.orig	2023-06-21 05:44:57.000000000 +0800
+++ CMakeLists.txt	2023-08-06 17:23:49.000000000 +0800
@@ -259,13 +259,6 @@
         endif()
     endif()
 
-    # only the default x86_64 or arm64 are supported
-    foreach (arch IN LISTS CMAKE_OSX_ARCHITECTURES)
-        if (NOT (arch STREQUAL "x86_64" OR arch STREQUAL "arm64"))
-            message(FATAL_ERROR "Invalid arch ${arch}")
-        endif()
-    endforeach()
-
     # configure Xcode templates
     set(XCODE_TEMPLATES_ARCH "\$(NATIVE_ARCH_ACTUAL)")
 endif()
