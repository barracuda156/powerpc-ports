From 9be5897e517399510215cb3d54a9ab087ac708c0 Mon Sep 17 00:00:00 2001
From: barracuda156 <vital.had@gmail.com>
Date: Wed, 27 Dec 2023 02:51:48 +0800
Subject: [PATCH 2/3] configure: fix for sysroot

---
 configure | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git configure configure
index b3b7528382..2c6b97f444 100755
--- configure
+++ configure
@@ -273,7 +273,7 @@ macSDKify()
 
     sdk=$(getSingleQMakeVariable "QMAKE_MAC_SDK" "$1")
     if [ -z "$sdk" ]; then echo "QMAKE_MAC_SDK must be set when building on Mac" >&2; exit 1; fi
-    sysroot=$(/usr/bin/xcodebuild -sdk $sdk -version Path 2>/dev/null)
+    sysroot="/Developer/SDKs/MacOSX10.6.sdk"
     if [ -z "$sysroot" ]; then echo "Failed to resolve SDK path for '$sdk'" >&2; exit 1; fi
 
     case "$sdk" in
-- 
2.43.0

